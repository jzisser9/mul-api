# Database Configuration
# Required for production deployment in Portainer or other container environments

# PostgreSQL Database Connection
DATABASE_HOST=your-postgres-host
DATABASE_USER=your-postgres-user
DATABASE_PASSWORD=your-postgres-password
DATABASE_PORT=5432

# Optional: Full database URL (alternative to individual variables above)
# DATABASE_URL=postgres://user:password@host:port/database_name

# Rails Configuration
RAILS_ENV=production
RAILS_LOG_TO_STDOUT=true
RAILS_SERVE_STATIC_FILES=true

# Optional: Seed database on startup (set to 'true' to enable)
RAILS_SEED_ON_STARTUP=false